# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'sensor_test' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
	pod "SwiftSocket"

  # Pods for sensor_test

  target 'sensor_testTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'sensor_testUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
